# Repairable calculator sample

This transparent sample intentionally contains one arithmetic implementation
defect in `calculator.add`. Its two tests define the expected behavior. Select
this directory in THEKEY and use **Escanear y reparar** to exercise diagnosis,
isolated repair, policy authorization, exact-byte application, backup, and
post-apply verification.
