## Context
Every run writes artifacts under `runs/<RUN_ID>/` (manifest, plan, approvals, changes.diff, gates.json, decision.json, artifact-hashes.json, evidence/). The event store is append-only and hash-chained.

## Problem
The artifact format is ad hoc and not trivially portable or machine-checkable by external tooling. A stable, documented export format would help auditors and downstream consumers.

## Scope
- Define a versioned, documented evidence bundle schema (JSON) that wraps the existing artifacts + event-chain tail.
- Add an export command (e.g. `thekey evidence export --run-id <ID>`) that emits the bundle without altering how the core records evidence.
- Keep `thekey evidence verify` working against both old and new formats.

## Out of scope
- Changing the state machine, gates, or event-store internals.
- Adding cryptographic signing (tracked separately as a future mitigation).

## Requisito o riesgo al que responde
- Requisito: THEKEY_CONTRACT §3 (Evidence is hash-sealed) — the export must preserve hashes and chain tail so external tooling can verify without the core.
- Riesgo cubierto: THREAT-T2 (Falsificación de evidencia) — a documented, verifiable bundle lets third parties detect tampering.

## Impacto si no se resuelve
Auditors cannot machine-check THEKEY output; the "auditable" claim relies on humans reading raw files, which does not scale.

## Acceptance criteria
- [ ] New schema file under `governance/schemas/` + a test that validates a real bundle.
- [ ] Export command produces a parseable bundle; `evidence verify` still passes on old and new formats.
- [ ] Docs note the format is additive (no core behavior change).

## Cómo se verifica
```bash
python -m thekey demo
python -m thekey evidence export --run-id <ID>
python -m thekey evidence verify --run-id <ID>
# expected: export + verify both succeed; demo decision RELEASE_ELIGIBLE, gates_passed: 4/4
```

## Evidencia en event store
This issue defines the export schema; the evidence it produces IS the artifact. On merge, the PR must show `Closes #5`, the export+verify output, and that the demo's `evidence_mismatches: []` (chain intact). The new bundle format becomes the canonical external evidence.

## Docs afectados
- [ ] README / README.en actualizado (evidence export section)
- [ ] CHANGELOG entrada añadida
- [ ] DECISIONS.md si hay decisión de diseño
