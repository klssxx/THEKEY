# Build Week video shot list

Target runtime: **2:48**. Acceptable range: **2:40–2:55**. Never exceed three
minutes.

| Shot | Time | Visual | Proof or narration anchor |
| --- | --- | --- | --- |
| 1 | 0:00–0:15 | Open `THEKEY.exe`; canonical home view | Exact name and tagline; the problem is governed verification, not merely running tests. |
| 2 | 0:15–0:35 | Main interface and three-step guidance | THEKEY core → THE KING orchestration without self-approval → CHECKMATE verdict without writes → scoped authority → PolicyEngine. |
| 3 | 0:35–1:20 | Select `SAMPLE-REPAIRABLE-PYTHON-APP`; **Reparar / Repair**; consent | Failed tests → redacted diagnosis → bounded mutation → full gates → backup → exact-byte apply → post-apply gates → `REPAIRED_AND_VERIFIED`. |
| 4 | 1:20–1:55 | Click **Demo para jueces** and hold on summary | Real `ALLOW: APPLIED, handlers=1`, adversarial `DENY: ROLE_NOT_ALLOWED, handlers=0`, `GATES: 4/4 PASS`, `DECISION: RELEASE_ELIGIBLE`. |
| 5 | 1:55–2:18 | **Ver resultados / View results**; safe structured fields only | Source hashes before/after, CHECKMATE, policy, gates and final verdict; bound judge receipts. Hide absolute path fields. |
| 6 | 2:18–2:40 | `git rev-parse HEAD`, delta, sanitized Codex evidence | Exact candidate binding; preexisting project versus verified delta; Codex with GPT-5.6 as development tooling. |
| 7 | 2:40–2:55 | Consent warning, then canonical title | Trusted tests only; isolated copy is not an OS sandbox; unsupported or untested projects are not called verified. |

## Screen preparation

- Extract the package to a short path and choose the included
  `SAMPLE-REPAIRABLE-PYTHON-APP` for the recorded product repair.
- Confirm `git rev-parse HEAD` equals the final candidate handoff.
- Pre-open the delta, sanitized Codex session record, and generated evidence.
- Use `THEKEY — THE KING OF CHECKMATE` as the public name. The commit SHA is a
  technical version identifier, never the title.
- Hide `source.path`, `test_execution.workspace`, `run_path`, and
  `workspace_path` because they can reveal a local user name.
- Show both runs continuously. Cuts may change windows between sections but
  must not make a prerecorded or edited result look live.
